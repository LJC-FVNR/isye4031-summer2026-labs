# Synthetic Customer Profile Data Dictionary

`simulated_customer_profiles.csv` is synthetic. It has different feature scales, a known latent segment, and a binary `high_value` label for PCA, KNN, and K-means demonstrations.
