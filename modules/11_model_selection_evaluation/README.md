# ISYE 4031 JupyterLite: Model Selection and Evaluation

This directory is the student-facing JupyterLite source for `11_model_selection_evaluation`.
Open `files/00_START_HERE.ipynb` first.

Build and validate from this directory:

```powershell
jupyter lite build --lite-dir .
jupyter lite check --lite-dir .
jupyter lite archive --lite-dir .
```

Only this directory should be copied to the public course repository under
`modules/11_model_selection_evaluation/`. Generated `_output/` files are build artifacts and
should not be committed.
