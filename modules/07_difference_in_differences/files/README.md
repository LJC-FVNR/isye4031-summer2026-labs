# Module 07 Files

Start with `00_START_HERE.ipynb`. The `data/` folder contains synthetic
teaching datasets used throughout the module.

These notebooks are designed for browser-based JupyterLite, but the same
files also run in a normal local Jupyter environment with the listed
scientific Python packages installed.
